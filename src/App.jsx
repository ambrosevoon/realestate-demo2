export default function App() {
  return (
    <div className="flex h-screen" style={{ background: 'var(--bg)' }}>
      <div style={{ color: 'var(--accent)', padding: '2rem' }}>
        SmartFlow Automation — Email Dashboard
      </div>
    </div>
  )
}
